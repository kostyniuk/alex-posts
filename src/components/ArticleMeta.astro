---
import { formatDate } from '../utils/articles';

export interface Props {
  title: string;
  publishDate: string;
  description?: string;
  readingTime: string;
  tags?: string[];
}

const props: Props = Astro.props;
const formattedDate = formatDate(props.publishDate, 'long');
---

<header class="mb-8 border-b border-gray-700 pb-6">
  <h1 class="text-4xl font-bold mb-4 text-[--text]">
    {props.title}
  </h1>
  
  <div class="flex flex-wrap items-center gap-4 text-sm text-gray-400 mb-4">
    <time datetime={props.publishDate}>
      {formattedDate} 📅
    </time>
    <span>•</span>
    <span>{props.readingTime} ⏱️</span>
  </div>
  
  {props.description && (
    <p class="text-lg text-gray-300 leading-relaxed mb-4">
      {props.description}
    </p>
  )}
  
  {props.tags && props.tags.length > 0 && (
    <div class="flex flex-wrap gap-2">
      {props.tags.map(tag => (
        <span class="px-4 py-2 bg-article-pink-400 text-article-pink-900 text-base font-medium rounded-full">
          {tag}
        </span>
      ))}
    </div>
  )}
</header> 