---
import { formatDate } from '../utils/articles';

export interface Props {
  title: string;
  publishDate: string;
  description?: string;
  slug: string;
  readingTime: string;
}

const props: Props = Astro.props;
const formattedDate = formatDate(props.publishDate, 'short');
---

<article class="border border-gray-200 dark:border-gray-700 rounded-lg p-6 hover:shadow-lg transition-shadow duration-200 cursor-pointer group">
  <a href={`/articles/${props.slug}`} class="block">
    <div class="flex items-center gap-2 text-sm text-gray-600 dark:text-gray-400 mb-3">
      <time datetime={props.publishDate}>
        {formattedDate} 📅
      </time>
      <span>•</span>
      <span>{props.readingTime} ⏱️</span>
    </div>
    
    <h2 class="text-xl font-semibold text-article-pink-400 mb-3 transition-all duration-500 ease-out group-hover:scale-104 group-hover:drop-shadow-2xl group-hover:shadow-white/30">
      {props.title}
    </h2>
    
    {props.description && (
      <p class="text-gray-700 dark:text-gray-300 mb-4 line-clamp-3">
        {props.description}
      </p>
    )}
  </a>
</article> 